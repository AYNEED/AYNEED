fragment commouUserFields on User {
  id
  isOnline
  about {
    skills {
      title
      primary
    }
  }
  personal {
    firstName
    lastName
    photo
  }
}

query GetUsers($cursor: String) {
  users(cursor: $cursor) {
    items {
      ...commouUserFields
    }
    hasMore
  }
}

subscription onUserAdded {
  userAdded {
    ...commouUserFields
  }
}
